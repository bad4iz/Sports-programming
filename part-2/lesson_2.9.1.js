/**
 * @module solution example 2.1.9
 * @param {number} m
 * @param {number} n
 * @returns {Array}
 */
export default ({ m, n }) => {
  const answer = [];

  // todo: тут решение

  return answer;
};
